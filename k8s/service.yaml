apiVersion: v1
kind: Service
metadata:
  name: nodejs-service
  namespace: jenkins
spec:
  type: NodePort
  selector:
    app: nodejs
  ports:
    - port: 80
      targetPort: 8085
      nodePort: 30021
